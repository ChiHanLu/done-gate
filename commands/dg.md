---
description: Shorthand for /done-gate — run the acceptance-gated wrap-up flow (plain-language summary + checklist verification).
---

Run the `done-gate` skill's acceptance flow now. This is an alias for `/done-gate`.

Pass through any arguments verbatim: $ARGUMENTS

Supports the same `log:` / `as:` / `lang:` arguments as `/done-gate`.
